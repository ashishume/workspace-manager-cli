# dependencies
node_modules/

# build output (generated by `npm run build` / `prepare`)
dist/

# runtime workspace created by the CLI
workspace/

# local repo config (repos.example.json is the committed template)
repos.json

# OS / editor
.DS_Store
.env
*.local

# Claude Code internal
.claude/
